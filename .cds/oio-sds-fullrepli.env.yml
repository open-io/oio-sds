name: oio-sds-env-fullrepli

values:
  MODEL:
    value: 'Ubuntu-20.04-VM-b2-60'

  CMAKE_OPTS:
    value: "-DENABLE_CODECOVERAGE=on -DCMAKE_INSTALL_PREFIX=/tmp/oio -DLD_LIBDIR=lib -DZK_LIBDIR=/opt/{{ .cds.env.ZOOKEEPER_PACKAGE }}/lib -DZK_INCDIR=/opt/{{ .cds.env.ZOOKEEPER_PACKAGE }}/include/zookeeper"

  G_DEBUG:
    value: "fatal_warnings"

  G_DEBUG_LEVEL:
    value: "W"

  LC_ALL:
    value: "C.UTF-8"

  PKG_CONFIG_PATH:
    value: "/tmp/oio/lib/pkgconfig"

  TEST_SUITE:
    value: "repli,with-service-id,with_tls,remote-account"

  ZK_CLUSTER:
    value: "1 2 3"

  ZK:
    value: "127.0.0.1:2191,127.0.0.1:2192,127.0.0.1:2193"

  OIO_SYSTEMD_SYSTEM:
    value: "1"
